# Local development values (minikube)
temporal:
  enabled: true

postgresql:
  enabled: true
  auth:
    postgresPassword: temporal
    database: temporal

secrets:
  local:
    enabled: true
    githubWebhookSecret: dev-secret
    githubToken: ""
  aws:
    enabled: false

rds:
  enabled: false

s3:
  enabled: false

nodePool:
  system:
    enabled: false
  ciJobs:
    enabled: false
