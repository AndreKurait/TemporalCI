{{- if .Values.nodePool.system.enabled }}
apiVersion: eks.services.k8s.aws/v1alpha1
kind: NodePool
metadata:
  name: system
  labels:
    {{- include "temporalci.labels" . | nindent 4 }}
spec:
  role: system
{{- end }}
